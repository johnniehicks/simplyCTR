"use strict";

var $ = require('jquery');

var App = {
  init: function init() {
    console.log('App initialized.');
  }
};

/*var App = {
  init: function init() {
    $('body').append('App initialized.');
  }
};*/

module.exports = App;
